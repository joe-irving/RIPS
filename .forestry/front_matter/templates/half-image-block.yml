---
label: Half Image Block
hide_body: true
fields:
- name: half_image
  type: file
  config:
    maxSize: 64
  label: Image
- name: text
  type: textarea
  default: ''
  config:
    required: false
    wysiwyg: false
    schema:
      format: markdown
  label: text
